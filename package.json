{
  "name": "expense-one",
  "version": "0.1.0",
  "description": "Spesenapp — expense tracker built with Vue 3, Pinia, Tailwind and Firebase (Auth, Firestore, Storage, Hosting)",
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && firebase deploy"
  },
  "dependencies": {
    "firebase": "^11.0.0",
    "pinia": "^2.2.0",
    "vue": "^3.5.0",
    "vue-router": "^4.4.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.0.0",
    "@vitejs/plugin-vue": "^5.1.0",
    "tailwindcss": "^4.0.0",
    "vite": "^6.0.0"
  }
}
