rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Each user can only read/write receipts under their own uid prefix.
    match /receipts/{userId}/{allFiles=**} {
      allow read, write:
        if request.auth != null
        && request.auth.uid == userId;
    }
  }
}
